# gcp-disable-default-logging
Shell script to disable GCP default log sink to prevent duplicate logging when centralized sink is in place.


### Change mode of Script before running
`chmod +x ./disable_log_sink.sh` 
